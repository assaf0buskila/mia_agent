# Gates: Owner boundary and typed reply loop

Scope: Harden the numeric owner mint, align the single owner-agent prompt/tools and exact call ceiling, standardize the owner reply port, and avoid owner-adapter construction on prospect traffic.

- [x] G1: `Principal.owner` is minted only after a non-empty numeric allowlist matches the inbound actor, before claim/persistence/tool/model work.
  EVIDENCE: `app/api/owner.py` now rejects empty, non-ASCII/non-numeric, and non-matching allowlists before minting; `process_owner_texts` and `process_inbound_texts` calculate the same match before webhook claiming. `test_owner_mint_rejects_empty_or_non_numeric_allowlists_before_side_effects` proves the direct legacy helper returns without touching its object dependencies.
- [x] G2: Owner prompt accurately describes bounded Sheets read/update/append and the parallel tool loop never exceeds the total-call ceiling.
  EVIDENCE: stale Sheets statement 1 -> 0; prompt now names `sheets_read`, explicit authenticated `sheets_update`/`sheets_append`, allowlisted IDs, and prohibited Drive/discovery/mutation behavior. `test_total_tool_call_ceiling_refuses_excess_calls_in_one_parallel_batch` sets ceiling 1, submits 3 calls, proves one execution, 2 refused result messages, and a tools-free next turn.
- [x] G3: Owner reply implementations share one async `compose` contract; caller-side awaitability inspection and dead compatibility/no-op owner code are removed.
  EVIDENCE: protocol plus Canned/Fake/OpenAI implementations are async; caller uses one direct await. `inspect.isawaitable` 1 -> 0; dead `calendar_booking` compatibility parameter 1 -> 0; owner no-op `pass` statements 2 -> 0; repeated due-at assignments 17 -> 1 named-set expression. Async Canned/Fake and OpenAI paths are covered by `test_owner_reply.py`.
- [x] G4: Prospect inbound does not construct owner-only provider adapters; the authenticated legacy owner branch retains equivalent behavior.
  EVIDENCE: eight owner-only builders moved from unconditional pre-loop construction to the numeric authenticated-owner branch. `test_prospect_inbound_never_constructs_owner_only_adapters` replaces every builder with a failure and passes a prospect event; existing injected legacy owner path regression remains in `test_inbound_owner_path_uses_injected_reply_port`.
- [x] G5: Focused owner, principal, Sheets, agent-loop, and inbound regressions pass with Ruff and diff-check.
  EVIDENCE: 2026-08-28: original leaf suite `68 passed, 83 warnings in 3.54s`; final clean-room Sheets/principal repair suite `95 passed, 74 warnings in 3.90s`, including no-port/no-claim/no-idempotency-row policy and argument rejections plus corrected same-event replay. Owned-path Ruff -> `All checks passed!`; `git diff --check` -> exit 0. Strict C901 reports only pre-existing `process_owner_item` (55) and `_website_kpis` (14), not the repaired binder.
- [x] G6: Sheets semantic binding rejects prohibited/ambiguous operations and exact payload subsets or supersets before a port, provider, or idempotency claim.
  EVIDENCE: 2026-08-28 `test_owner_sheets_semantic_binding_rejects_negation_conflicts_and_payload_mismatches` covers the fourth-review English negated-append and two-literal subset reproductions; English `don't`/`don’t`/`dont`, Hebrew `אל תוסיף`/`לא להוסיף`/`אל תכניס`/`לא להכניס`/`אל תעדכן`/`לא לעדכן`/`אל תמלא`/`לא למלא`, affirmative append/update conflicts, a two-target/two-value request, and a strict supplied-value superset. Every denial observes `port == 0`, `claim == 0`, zero `IdempotencyRow`, and zero fake-provider operations. The corrected original event writes once and exact replay remains idempotent. `uv --offline --cache-dir .uv-cache run pytest -p no:cacheprovider --basetemp .pytest-sheets-semantic-negation-symmetry tests/unit/test_owner_sheets.py tests/unit/test_owner_live_tools.py tests/unit/test_sheets.py tests/unit/test_vnext_principal.py` -> `96 passed, 74 warnings in 4.09s`; owned Ruff passed; strict C901 reports only existing `_website_kpis` (14); `git diff --check` -> exit 0 with pre-existing CRLF warnings only.
- [x] G7: One current owner turn binds exactly one unquoted allowlisted spreadsheet ID/A1 target pair, and shared value normalization rejects empty results.
  EVIDENCE: 2026-08-28 real-registry/SQLite/FakeSheets tests deny the fifth-review two-target case, same-ID/two-range, two-ID/same-range, different-tab/same-core range, quoted targets, and token collisions with `port == 0`, `claim == 0`, zero `IdempotencyRow`, and zero provider operations. Corrected same-event replay writes once. Shared normalization rejects `"   "` before HTTP; `"x  y"` is sent unchanged internally. `uv --offline --cache-dir .uv-cache run pytest -p no:cacheprovider --basetemp .pytest-sheets-fifth-repair-final tests/unit/test_owner_sheets.py tests/unit/test_owner_live_tools.py tests/unit/test_sheets.py tests/unit/test_vnext_principal.py` -> `98 passed, 74 warnings in 4.03s`; owned Ruff passed; strict C901 reports only existing `_website_kpis` (14); `git diff --check` -> exit 0 with pre-existing CRLF warnings only.
