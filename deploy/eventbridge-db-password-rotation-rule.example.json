{
  "Name": "mia-db-password-rotated",
  "Description": "Chunk C9: fires when the RDS-managed secret finishes rotating the master password, so mia-db-password-sync can push it to mia/prod. Verify the exact eventName against AWS CloudTrail/Secrets Manager docs before deploying -- not exercised against real AWS in this repo.",
  "State": "ENABLED",
  "EventPattern": {
    "source": ["aws.secretsmanager"],
    "detail-type": ["AWS API Call via CloudTrail"],
    "detail": {
      "eventSource": ["secretsmanager.amazonaws.com"],
      "eventName": ["RotationSucceeded"],
      "requestParameters": {
        "secretId": [
          "arn:aws:secretsmanager:REGION:ACCOUNT_ID:secret:rds!db-d7c051e7-2f6a-4711-826d-2bf7d243a2f8-gjs5XD"
        ]
      }
    }
  }
}
