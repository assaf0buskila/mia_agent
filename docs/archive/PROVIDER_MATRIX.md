# Provider capability matrix

Living inventory. Not application config. Prices, quotas, beta flags, and OAuth scopes must be re-read from the official page before production. Do not copy observed prices into Python constants.

**Last matrix pass:** 2026-08-22  
**Bible baseline:** v1.1 (validated 21 August 2026)  
**Production adapter map:** ADR-015 in `docs/DECISIONS.md`. Env comments: `.env.example`. Box keys: `deploy/mia-prod.secret.example.json`.  
**Implementation status:** Telegram owner bot is **alive** (`POST /v1/telegram/webhook`; numeric allowlist; voice in / text out; ADR-017). WhatsApp inbound is Meta Cloud HMAC (`app/api/whatsapp.py`) with production `MIA_WHATSAPP_REQUIRE_BUSINESS_SCOPE=true` (unknown/personal contacts stay human-only). Outbound text send is **alive** via `MessagePort`: Graph when `MIA_WHATSAPP_SENDER=direct` (default); Composio pin `WHATSAPP_SEND_MESSAGE` toolkit `20260815_00` when sender=`composio` (ADR-016). Never dual-send. Official Composio WhatsApp toolkit has **no incoming-message trigger**; `WHATSAPP_GET_MESSAGE_HISTORY` is delivery-status audit, not an inbox; template send is not wired. WhatsApp media download for voice-note STT is alive (in-memory Graph; allowlisted hosts; skipped for personal/unknown when the business-scope gate is on). GPT Transcribe STT port is alive (`app/integrations/transcribe.py`; model from `MIA_OPENAI_TRANSCRIBE_MODEL`, default `gpt-transcribe`). Instagram direct Graph DM send/webhook is alive (`app/integrations/instagram.py`, host `graph.instagram.com`); Composio `INSTAGRAM_SEND_TEXT_MESSAGE` is pinned (toolkit `20260819_00`) when sender=`composio`; default sender stays `direct`; organic/referral attribution on inbound prospect path is alive (`sanitize_instagram_attribution`; story id + referral/ad ids only; no CDN/media stored). Instagram organic content insights read is alive (`app/integrations/instagram_insights.py`; Graph when sender=`direct` and tokens set; Composio `GET_IG_USER_MEDIA` + `GET_IG_MEDIA_INSIGHTS` toolkit `20260819_00` when sender=`composio` or Graph tokens empty; Postgres `content_insights`; owner analytics ack; Sheets tab `07`). ManyChat External Request ingest is alive (`app/integrations/manychat.py`, `POST /v1/manychat/external-request`; bearer `MIA_MANYCHAT_INGEST_TOKEN`; ingest-only; Dynamic Block v2 when `MIA_INSTAGRAM_SENDER=manychat`; no Composio; no Public API send). Composio Gmail inbound ingest is alive (`app/api/composio.py`, `app/integrations/gmail.py`; toolkit `GMAIL` version `20260817_00`, trigger `GMAIL_NEW_GMAIL_MESSAGE`; poll latency ~1–15 min acceptable this slice). Calendar free/busy read is alive (`app/integrations/calendar.py`; typed `CalendarPort`; live Composio HTTP execute when `MIA_COMPOSIO_API_KEY` + `MIA_COMPOSIO_USER_ID` set; `DisabledCalendarPort` otherwise; enriches `OFFER_MEETING` on inbound + website; owner WhatsApp calendar availability read via `app/domain/owner_calendar.py` — same `GOOGLECALENDAR_FIND_FREE_SLOTS` pin, ADR-012 carve, numbered Hebrew ack, no busy-event dump; Composio pin `GOOGLECALENDAR_FIND_FREE_SLOTS` on toolkit `GOOGLECALENDAR` version `20260812_00`; no create/delete/triggers this slice). Google Sheets lead mirror is alive (`app/integrations/sheets.py`; typed `SheetsPort`; live Composio HTTP execute when `MIA_COMPOSIO_API_KEY` + `MIA_COMPOSIO_USER_ID` + `MIA_SHEETS_SPREADSHEET_ID` set; `DisabledSheetsPort` otherwise; upserts `01 Leads` after graph on inbound + website; Composio pin `GOOGLESHEETS_UPSERT_ROWS` on toolkit `GOOGLESHEETS` version `20260813_00`; Postgres SoR; no sheet→CRM read; no other tabs; no clear/delete/create-spreadsheet/`VALUES_GET`/triggers this slice). Meta Ads insights read is alive (`app/integrations/meta_ads.py`; typed `MetaAdsPort`, default `DisabledMetaAdsPort`; enriches owner analytics ack on WhatsApp owner audio; Composio pin `METAADS_GET_INSIGHTS` on toolkit `METAADS` version `20260731_00` when live credentials land; Assaf-owned Meta credentials; managed app **No**; 0 triggers; missing metrics not zero-filled; no create/update/delete/pause this slice). Research public search read is alive (`app/integrations/research.py`; typed `ResearchPort`; `FirecrawlSearchPort` live when `MIA_FIRECRAWL_API_KEY` set; `DisabledResearchPort` otherwise; enriches owner research ack on WhatsApp owner audio; snippets are data; non-https dropped; no crawl/browser/LLM this slice). LinkedIn own-profile read is alive (`app/integrations/linkedin.py`; typed `LinkedInPort`, default `DisabledLinkedInPort`; enriches owner linkedin ack on WhatsApp owner audio; Composio pin `LINKEDIN_GET_MY_INFO` on toolkit `LINKEDIN` version `20260724_00` when live OAuth lands; managed app **Yes**; 0 triggers; no post/comment/delete/DM/upload this slice). Graph Lab local eval harness is alive (`app/evals`; fixture dataset `sales_v1`; deterministic replay of `select_next_action` + `reply_for`; no LangSmith SDK this slice). Composio WhatsApp toolkit catalogued (**unused for inbound**, ADR-015/016). Other provider rows remain planned until their ports exist.

**ADR-013 calendar update:** Create and reschedule are alive by fake through the separate typed `CalendarBookingPort`. Same toolkit/version pin `GOOGLECALENDAR` `20260812_00`; reschedule adds exact-event read `GOOGLECALENDAR_EVENTS_GET` and narrow write `GOOGLECALENDAR_PATCH_EVENT`. PATCH is limited to `calendar_id`, `event_id`, `start_time`, `end_time`, `timezone`, and `send_updates=none`; mandatory GET verification is authoritative. Cancellation is a Postgres request for Assaf and makes no provider call. `GOOGLECALENDAR_UPDATE_EVENT`, provider delete/cancel tools, attendees, descriptions, and conference mutation are not allowlisted. Real staging OAuth CREATE/PATCH/GET remains an operator acceptance action.

## How to read

| Check | Meaning |
| --- | --- |
| confirmed | Official page fetched or catalog row seen this pass |
| stale-url | Bible link no longer resolves; use the replacement URL |
| not-fetched | Bible citation only; revalidate before that integration is coded |

## Core runtime

| Provider | Mia use | Role | Check | Official source | Notes |
| --- | --- | --- | --- | --- | --- |
| LangGraph | Orchestration, checkpoints, interrupts, subgraphs | Core | confirmed (interrupts) | [Interrupts](https://docs.langchain.com/oss/python/langgraph/interrupts) | Interrupt payloads must be JSON-serializable. Side effects before `interrupt()` must be idempotent because the node re-runs on resume. Persistence page [R22] timed out this pass — re-fetch before checkpointer choice. |
| LangSmith | Eval datasets and sales quality loop | Core (Graph Lab) | not-fetched | [Evaluation](https://docs.langchain.com/langsmith/evaluation) | Future Sales Engine quality loop; local Graph Lab harness (`app/evals`, dataset `sales_v1`) does not require LangSmith this slice. |
| PostgreSQL | System of record | Core | confirmed (ADR-014) | RDS PostgreSQL for first live; app pin `postgresql+psycopg://` | Sheets is a mirror only. Sqlite is laptop-only. |
| AWS | Ingress, secrets, queues, production runtime | Core, first live host chosen | confirmed (ADR-014) | [Secrets Manager encryption](https://docs.aws.amazon.com/secretsmanager/latest/userguide/security-encryption.html); [ECS secrets as env](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/secrets-envvar-secrets-manager.html) | First live: Fargate + RDS + SM `mia/prod` + ALB `mia.assafweb.com`. AgentCore is a later candidate, not a lock-in. Lambda is not the long-workflow runtime and not the key box. |
| FastAPI | HTTP API | Core | — | — | Standard library choice from Bible; no vendor revalidation needed. |

## Model router (runtime Mia, not Cursor)

Do not hard-code these IDs as permanent brains. Router is eval-driven config.

| Model | Bible task class | Check | Official source | Notes |
| --- | --- | --- | --- | --- |
| GPT-5.6 Luna (`gpt-5.6-luna`) | Normal sales; cheap extraction | confirmed | [Models catalog](https://developers.openai.com/api/docs/models) and [Luna](https://developers.openai.com/api/docs/models/gpt-5.6-luna) | Still listed as the cost-sensitive GPT-5.6 tier. Supports function/tool use. Pricing changed (OpenAI announced an 80% Luna cut on 30 Jul 2026). Read live pricing before launch. Live sales paraphrase uses OpenAI Chat Completions (`OpenAISalesReplyPort`) when `MIA_OPENAI_API_KEY` and `MIA_SALES_MODEL` or `MIA_SALES_FALLBACK_MODEL` are set; model ids stay env/eval config — never hard-coded. Primary failure retries OpenAI fallback once, then Gemini AI Studio OpenAI-compat (`MIA_GEMINI_API_KEY` + `MIA_SALES_GEMINI_MODEL`, host `generativelanguage.googleapis.com`) once, then canned. Empty all = canned. Kill switch = canned, no HTTP. |
| GPT Transcribe | Voice-note STT | confirmed | [GPT Transcribe](https://developers.openai.com/api/docs/models/gpt-transcribe) | File transcription via `OpenAITranscribePort` (`MIA_OPENAI_TRANSCRIBE_MODEL`, default `gpt-transcribe`; optional `MIA_OPENAI_TRANSCRIBE_FALLBACK_MODEL` on primary failure). No Mia voice **output**. |
| Gemini (`gemini-3.6-flash`) | Sales paraphrase fallback | confirmed | [OpenAI compatibility](https://ai.google.dev/gemini-api/docs/openai) | AI Studio key `MIA_GEMINI_API_KEY` + `MIA_SALES_GEMINI_MODEL`. Same Chat Completions JSON as OpenAI on `https://generativelanguage.googleapis.com/v1beta/openai/chat/completions`. Not Vertex. Not Gmail/Calendar. Human Voice lint still applies; fail → canned. |
| Grok 4.6 | Deep research / long-horizon | confirmed | [Introducing Grok 4.6](https://x.ai/news/grok-4-6) (12 Aug 2026) | Positioned for long-running agents. API available. Observed list price on that page: $2 / $6 per 1M in/out; fast variant 2×. Put in config, not code. |

## Tool adapters

| Provider | Mia use | Role | Check | Official source | Notes |
| --- | --- | --- | --- | --- | --- |
| Composio (catalog) | Primary tool supplier behind typed adapters | Adapter | confirmed | [Toolkits](https://docs.composio.dev/toolkits) | Catalog now **1326** toolkits (Bible [R2] said 1181). Does not change the adapter rule. Do not load the catalog into the model. Pin production tool versions. |
| Composio Instagram | IG send + organic insights (ADR-015) | Adapter (wired; default sender still `direct`) | confirmed | [Instagram toolkit](https://docs.composio.dev/toolkits/instagram) slug `INSTAGRAM` version **`20260819_00`**. Pins: **`INSTAGRAM_SEND_TEXT_MESSAGE`**, **`INSTAGRAM_GET_IG_USER_MEDIA`**, **`INSTAGRAM_GET_IG_MEDIA_INSIGHTS`**. **No inbound DM trigger.** | Keep Graph tokens for webhook verify and default-direct send. One IG sender. Do not flip production `MIA_INSTAGRAM_SENDER` to `composio` until staging send is tested. |
| Composio Gmail | Lead email ingest (no send this slice) | Adapter | confirmed | [Gmail toolkit](https://docs.composio.dev/toolkits/gmail) | Slug `GMAIL`, version **`20260817_00`**, 63 tools, 2 triggers, managed **Yes**. Inbound via signed Composio webhook (`POST /v1/composio/webhook`) on trigger `GMAIL_NEW_GMAIL_MESSAGE` (poll; ~1–15 min latency acceptable this slice). Ignore `GMAIL_EMAIL_SENT_TRIGGER`. Typed `GmailPort`; pin `GMAIL_FETCH_MESSAGE_BY_MESSAGE_ID` same version for empty-body hydrate when `MIA_COMPOSIO_API_KEY` + `MIA_COMPOSIO_USER_ID` set; canonical `conversation_id`=Gmail `thread_id`; lead identity=sender email. Never execute send/delete tools (`GMAIL_SEND_EMAIL`, `GMAIL_DELETE_*`, `GMAIL_BATCH_DELETE_MESSAGES`). Do not dump the 63-tool catalog into the model. |
| Composio Google Calendar | Free/busy, owner availability read, explicit create, exact reschedule (ADR-011/012/013) | Adapter | confirmed | [Google Calendar toolkit](https://docs.composio.dev/toolkits/googlecalendar) | Slug `GOOGLECALENDAR`, version **`20260812_00`**, managed **Yes**. Pins: **`GOOGLECALENDAR_FIND_FREE_SLOTS`**, **`GOOGLECALENDAR_EVENTS_LIST`**, **`GOOGLECALENDAR_CREATE_EVENT`**, **`GOOGLECALENDAR_EVENTS_GET`**, **`GOOGLECALENDAR_PATCH_EVENT`**. Separate typed read `CalendarPort` and write/readback `CalendarBookingPort`. Owner WhatsApp calendar availability uses same `FIND_FREE_SLOTS` pin via `app/domain/owner_calendar.py` (ADR-012 carve; no busy-event list). Create uses deterministic private booking key, conflict recheck, and verify. Reschedule uses exact stored event ID, ADR-012 revalidation, exact conflict recheck, narrow PATCH, mandatory exact GET verification, and idempotent local recovery when provider is already at target. Create/reschedule alive by fake; live write/readback needs operator OAuth acceptance. Cancellation is local request only; provider delete remains R5 denied. Never use `GOOGLECALENDAR_UPDATE_EVENT`, attendees, description, or event-content mutation for reschedule. No Calendar triggers. |
| Composio Google Sheets | Lead + deal + source + follow-up + weekly KPI + activity + content + campaign budget/performance mirror (`01`–`10` upsert; no read-back) | Mirror adapter | confirmed | [Google Sheets toolkit](https://docs.composio.dev/toolkits/googlesheets) | Slug `GOOGLESHEETS`, version **`20260813_00`**, 53 tools, 16 triggers, managed **Yes**. Pin **`GOOGLESHEETS_UPSERT_ROWS`** only (`01 Leads`/`02 Campaign Budget`/`03 Campaign Performance` key `Campaign`/`04 Meetings`/`05 Deals`/`06 Lead Sources`/`07 Content Performance` key `Media ID`/`08 Follow-ups` key `Lead ID`; `09 Weekly KPI` key `Week Start`; `10 Mia Activity` key `Run ID`). Do not use deprecated `GOOGLESHEETS_BATCH_UPDATE` / SQL / `SHEET_FROM_JSON`. Typed `SheetsPort` in `app/integrations/sheets.py`; live Composio HTTP execute when `MIA_COMPOSIO_API_KEY` + `MIA_COMPOSIO_USER_ID` + `MIA_SHEETS_SPREADSHEET_ID` set; `DisabledSheetsPort` otherwise; `FakeSheetsPort` for tests. Source + weekly KPI on website session create; lead/deal/follow-up/activity/KPI after graph + calendar enrich on inbound (prospect) + website; tabs `02`/`03` on owner analytics path when `MIA_CAMPAIGN_MONTHLY_BUDGET` set; tab `07` on owner analytics Instagram insights path; deal mirror stage-only with empty expected/closed values; activity row from `ai_runs` after persist; KPI counts from canonical events (ISO Monday week) + pending follow-up snapshot — no spend. R1 `sheets_mirror` before port call. Never execute clear/delete/create-spreadsheet/find-replace/batch-clear/`VALUES_GET` tools this slice. No Composio Sheets triggers (inbound from sheet would invert SoR). Postgres is SoR — never read sheet into SalesState. |
| Composio Meta Ads | Campaign insights read (no writes this slice) | Adapter | confirmed | [Meta Ads toolkit](https://docs.composio.dev/toolkits/metaads) | Slug `METAADS`, version **`20260731_00`**, 53 tools, **0 triggers**, managed app **No**. Assaf-owned Meta credentials required. Pin **`METAADS_GET_INSIGHTS`** only. Official `date_preset` values include `today`, `last_7d`, `last_30d`, `this_month`; custom `time_range` valid when `date_preset` omitted — never both. Typed `MetaAdsPort` in `app/integrations/meta_ads.py`; live Composio HTTP execute when `MIA_COMPOSIO_API_KEY` + `MIA_COMPOSIO_USER_ID` + `MIA_META_ADS_ACCOUNT_ID` set; `DisabledMetaAdsPort` otherwise; `FakeMetaAdsPort` for tests. Enriches owner analytics ack on WhatsApp owner audio after classify+log (`last_7d` + previous 7d `time_range` + read-only `today` vs baseline 7d `time_range` + when 7d is watch `last_30d` + previous 30d `time_range`; optional `this_month` when budget configured). Never send `date_preset` and `time_range` together. R0 `meta_ads_read` before port call. Missing metrics omitted, never zero-filled. Never execute `METAADS_CREATE_*`, `METAADS_UPDATE_*`, `METAADS_DELETE_CAMPAIGN`, `METAADS_UPLOAD_AD_IMAGE`, or status/budget writes this slice. |
| Composio LinkedIn | Own-profile read (managed OAuth) | Secondary adapter | confirmed | [LinkedIn toolkit](https://docs.composio.dev/toolkits/linkedin) | Slug `LINKEDIN`, version **`20260724_00`**, 22 tools, 0 triggers, managed **Yes**. Pin **`LINKEDIN_GET_MY_INFO`** only. Typed `LinkedInPort` in `app/integrations/linkedin.py`; live Composio HTTP execute when `MIA_COMPOSIO_API_KEY` + `MIA_COMPOSIO_USER_ID` set; `DisabledLinkedInPort` otherwise; `FakeLinkedInPort` for tests. Enriches owner linkedin ack profile line after classify+log. R0 `linkedin_read` before port call. Never execute `LINKEDIN_CREATE_LINKED_IN_POST`, `LINKEDIN_CREATE_ARTICLE_OR_URL_SHARE`, `LINKEDIN_CREATE_COMMENT_ON_POST`, `LINKEDIN_DELETE_*`, `LINKEDIN_INITIALIZE_IMAGE_UPLOAD`, or `LINKEDIN_REGISTER_IMAGE_UPLOAD` this slice. **`LINKEDIN_GET_SHARE_STATS` is organization-page analytics (organization URN required) — not used for Assaf's personal member profile.** LinkedIn Ads is a separate toolkit — do not touch. No post/DM this slice. |
| LinkedIn REST (direct) | Personal member post analytics read | Primary for §21A analytics | confirmed | [Member post statistics](https://learn.microsoft.com/en-us/linkedin/marketing/community-management/members/post-statistics?view=li-lms-2026-08) | Separate typed `LinkedInAnalyticsPort` in `app/integrations/linkedin_analytics.py` (ADR-009). Pin **`LINKEDIN_API_VERSION=202608`**. `GET https://api.linkedin.com/rest/memberCreatorPostAnalytics` with `q=me`, one `queryType` per request, `aggregation=TOTAL`, previous 30 completed local-calendar days. OAuth scope **`r_member_postAnalytics`** (3-legged member OAuth; app approval required). Live when `MIA_LINKEDIN_ACCESS_TOKEN` set; `DisabledLinkedInAnalyticsPort` otherwise; `FakeLinkedInAnalyticsPort`/`DirectLinkedInAnalyticsPort` in tests. Enriches owner linkedin ack stats line after profile enrich. R0 `linkedin_analytics_read`. Six metrics only; no post content/URLs/member IDs/raw response in ack or canonical events. Not Composio. No OAuth callback/refresh this slice. |
| Composio ManyChat | — | **Do not use** | confirmed (deprecation) | [Changelog index](https://docs.composio.dev/changelog/2025/9/15) lists 19 Dec 2025 toolkit deprecation including `MANY_CHAT` | Bible [R10] URL `docs.composio.dev/reference/changelog` is **404**. Direct ManyChat API only. |
| ManyChat | Optional IG trigger / engagement | Optional sidecar | confirmed (external request ingest) | [External request](https://help.manychat.com/hc/en-us/articles/14281285374364-Dev-Tools-External-request), [Dynamic Block v2](https://manychat.github.io/dynamic_block_docs/channels/) | Never the brain. One sender owns the conversation. Alive: `POST /v1/manychat/external-request` bearer ingest; Dynamic Block v2 when `MIA_INSTAGRAM_SENDER=manychat`. No Composio `MANY_CHAT`. No Public API send this slice. |
| Make | Non-critical glue | Optional sidecar | not-fetched | Make AI Agent still cited as open beta [R19] | Not the orchestrator. Signed HTTPS only. |
| Composio WhatsApp | — | **Unused (ADR-015)** | confirmed | [WhatsApp toolkit](https://docs.composio.dev/toolkits/whatsapp) | Do not wire. Inbound stays Meta webhook (ADR-006). Trigger is a documented no-op poll. |
| Telegram Bot API | Private owner control (Assaf only) | Channel adapter | confirmed (webhook + sendMessage + getFile) | [setWebhook](https://core.telegram.org/bots/api#setwebhook) | ADR-017. `POST /v1/telegram/webhook`. Numeric user-id allowlist. Voice notes download from allowlisted `api.telegram.org` then STT. No TTS. Unauthorized users ignored (HTTP 200). |
| Meta WhatsApp Cloud API | Verified business continuation only | Channel adapter (direct Graph API behind typed port) | confirmed (send text + media download) | [WhatsApp text messages](https://developers.facebook.com/docs/whatsapp/cloud-api/messages/text-messages/) | Live ingress. Production unknown/personal contacts stay human-only (ADR-017). Website handoff token continues the same lead. Voice-note STT downloads media in memory then transcribes when automation is allowed. Owner WhatsApp phones remain a fallback allowlist. No templates in this slice. |
| Meta Instagram API | IG inbound webhook + Graph send/insights when sender=`direct` | Channel adapter (Instagram Login, `graph.instagram.com`) | confirmed (send/webhook/attribution/insights read) | [Instagram messaging API](https://developers.facebook.com/docs/instagram-platform/instagram-api-with-instagram-login/messaging-api/) | Inbound HMAC stays here (ADR-015). Mia Graph sender when `MIA_INSTAGRAM_SENDER=direct`. Organic/referral attribution alive on inbound. Content insights read alive on owner analytics ack. Token needs `instagram_business_manage_insights`. ManyChat sidecar ingest alive; no dual-send. |
| Google Sheets API | Operating mirror | Mirror | confirmed | [Sheets concepts](https://developers.google.com/workspace/sheets/api/guides/concepts) | Not the CRM. Mia upserts `01`/`06`/`08`/`09`/`10` via Composio pin above. Preview Sheets MCP stays feature-flagged [R35]. |
| Firecrawl | Public research search + homepage SEO scrape audit | Research + SEO audit adapter | confirmed | [Search](https://docs.firecrawl.dev/features/search); [Scrape v2](https://docs.firecrawl.dev/features/scrape) | Search: `POST /v2/search` via `FirecrawlSearchPort`. Scrape: `POST /v2/scrape` via `SeoAuditPort` allowlisted to `www.assafweb.com` / `assafweb.com` only; derived facts only (title, description, canonical, h1_count, has_json_ld); no Postgres HTML dump. |
| Composio Google Search Console | Search analytics + URL inspect read | Adapter | confirmed | [Search Console toolkit](https://docs.composio.dev/toolkits/google_search_console) | Slug `GOOGLE_SEARCH_CONSOLE`, version **`20260806_00`**. Pins: **`SEARCH_ANALYTICS_QUERY`**, **`INSPECT_URL`**, **`LIST_SITES`** only. Typed `SearchConsolePort`; live when Composio key+user + `MIA_GSC_SITE_URL`; owner SEO ack enrich; R0 read; never add/submit sitemap tools. |
| Composio Google Analytics | GA4 pivot + conversion list read | Adapter | confirmed | [Google Analytics toolkit](https://docs.composio.dev/toolkits/google_analytics) | Slug `GOOGLE_ANALYTICS`, version **`20260721_00`**. Pins: **`RUN_PIVOT_REPORT`**, **`LIST_CONVERSION_EVENTS`** only. Property `properties/{digits}` via `MIA_GA4_PROPERTY_ID`; never **`SEND_EVENTS`** / Measurement Protocol. |
| Apify | Public research (secondary) | Research | not-fetched | Bible §21 | Behind `ResearchPort` when needed; **not wired**. Do not add `MIA_APIFY_*` until the adapter exists (ADR-015). Composio also lists an Apify toolkit — still behind our research adapter. |
| Playwright | Browser fallback | Restricted fallback | — | — | Allowlist, read-only default, injection controls. |

## Website and AssafWeb

| Provider | Mia use | Role | Check | Official source | Notes |
| --- | --- | --- | --- | --- | --- |
| AssafWeb on Vercel | Session/events, Mia widget, handoff tokens | Core channel | not-fetched | https://www.assafweb.com/ [R1] | Custom API client. First channel after foundation. |

## Stale Bible links found this pass

| Bible ref | Old URL | What to use |
| --- | --- | --- |
| [R10] | `https://docs.composio.dev/reference/changelog` (404) | `https://docs.composio.dev/changelog/2025/9/15` and dated entries under `/docs/changelog/` |

If this table disagrees with `app/core/capabilities.py`, the code wins until the PRD is updated in the same turn.

## Explicitly out of this file

- Runtime price tables copied into code
- Full Composio catalog dump
- Cursor build-time model policy (that lives in `AGENTS.md`)
- Credential values
